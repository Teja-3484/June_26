class Solution:
    def largestAltitude(self, gain: List[int]) -> int:
        alt=0
        ans=[]
        ans.append(alt)
        for i in range(len(gain)):
            alt+=gain[i]
            ans.append(alt)
        return max(ans)
