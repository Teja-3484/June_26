class Solution:
    def captureForts(self, forts: List[int]) -> int:
        ans = k = 0 
        for i, x in enumerate(forts): 
            if x: 
                if forts[k] == -x: ans = max(ans, i-k-1)
                k = i 
        return ans 
