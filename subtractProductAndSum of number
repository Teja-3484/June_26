class Solution:
    def subtractProductAndSum(self, n: int) -> int:
        p=1
        s=0
        while n>0:
            rem=n%10
            s+=rem
            p*=rem
            n=n//10
        return (p-s)
